import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Mineable Coins • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Mineable Coins"
      subtitle="See which coins are mineable by algorithm and how coin economics can shift profitability snapshots over time."
    />
  );
}
