import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Mining Pools • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Mining Pools"
      subtitle="A practical overview of mining pools, payout methods, and pool selection trade-offs for ASIC operators."
    />
  );
}
