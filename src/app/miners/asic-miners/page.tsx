import { PageShell } from "@/components/page-shell";

export const metadata = { title: "ASIC Miners • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="ASIC Miners"
      subtitle="Explore ASIC hardware with structured specifications, vendor listings, and profitability snapshots built for real purchasing decisions."
    />
  );
}
