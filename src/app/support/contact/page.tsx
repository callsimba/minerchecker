import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Contact Us • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Contact Us"
      subtitle="Contact MinerChecker for vendor verification, data corrections, partnerships, or support requests."
    />
  );
}
