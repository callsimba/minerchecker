import { PageShell } from "@/components/page-shell";

export const metadata = { title: "FAQ • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="FAQ"
      subtitle="Answers to common questions about profitability snapshots, vendor listings, trust signals, and platform behavior."
    />
  );
}
