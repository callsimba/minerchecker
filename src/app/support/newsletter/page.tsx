import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Newsletter • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Newsletter"
      subtitle="Get product updates, mining market summaries, and new feature announcements. This page will expand into a full signup flow."
    />
  );
}
