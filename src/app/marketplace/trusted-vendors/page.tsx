import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Trusted Vendors • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Trusted Vendors"
      subtitle="Vendor verification and trust signals help reduce risk when purchasing miners. Listings remain manual and auditable."
    />
  );
}
