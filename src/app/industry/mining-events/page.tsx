import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Mining Events • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Mining Events"
      subtitle="Industry calendar for major mining conferences, hardware launches, and network events relevant to ASIC operators."
    />
  );
}
