import { PageShell } from "@/components/page-shell";

export const metadata = { title: "Halving Countdown • MinerChecker" };

export default function Page() {
  return (
    <PageShell
      title="Halving Countdown"
      subtitle="Halvings affect mining economics, hash rate competition, and hardware ROI. This page will expand into a live countdown and impact model."
    />
  );
}
